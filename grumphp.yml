parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        composer: ~
        phpcs:
            tab_width: 4
            ignore_patterns:
                - "dev"
        phpcsfixer:
            level: psr2
        phpunit:
          config_file: "src/dev/tests/unit/phpunit.xml"
